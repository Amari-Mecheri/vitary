// Navbar.js
import './Navbar.css';

const Navbar = () => {
  return (
    <nav className="navbar">
      <a href="#">Home</a>
      <a href="#">Profile</a>
      <a href="#">Logout</a>
    </nav>
  );
};

export default Navbar;
